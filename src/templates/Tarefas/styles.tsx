import styled from "styled-components"

const TarefasWrapper = styled.div`
  display: flex;
  flex-direction: column;
  height: 100vh;
  overflow: hidden;
  justify-content: space-between;
`

export { TarefasWrapper }
