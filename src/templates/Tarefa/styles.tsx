import styled from "styled-components"

const TarefaWrapper = styled.div`
  display: flex;
  flex-direction: column;
  height: 100vh;
  overflow: hidden;
  justify-content: space-between;
`

export { TarefaWrapper }
